import * as React from 'react';
// import { css } from 'emotion';
import styled from 'react-emotion';

const DispatchItem = (props) => (
    <div>
        <h1>SUCK THIS</h1>
        <CDispatchItem/>
    </div>
);

const CDispatchItem = styled('div')`
    CSS GOES HERE
`;

export default DispatchItem;